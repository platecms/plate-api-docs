# API Usage

The following sections describe how to connect to the Plate API.

<% current_page.data.guides && current_page.data.guides.each do |resource| %>
  <%= partial "guides/#{resource}" %>
<% end %>
